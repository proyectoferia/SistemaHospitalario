🚨 EJECUTAR ESTOS COMANDOS EN phpMyAdmin 🚨

COPIA Y PEGA EXACTAMENTE ESTOS COMANDOS UNO POR UNO:

1️⃣ Seleccionar base de datos:
USE citas_medicas;

2️⃣ Eliminar clave foránea:
ALTER TABLE events DROP FOREIGN KEY events_ibfk_1;

3️⃣ Modificar tabla patients:
ALTER TABLE patients MODIFY idpa BIGINT(20) NOT NULL AUTO_INCREMENT;

4️⃣ Modificar tabla events:
ALTER TABLE events MODIFY idpa BIGINT(20) NOT NULL;

5️⃣ Recrear clave foránea:
ALTER TABLE events ADD CONSTRAINT events_ibfk_1 FOREIGN KEY (idpa) REFERENCES patients(idpa) ON DELETE CASCADE ON UPDATE CASCADE;

6️⃣ Resetear contador:
ALTER TABLE patients AUTO_INCREMENT = 1;

✅ DESPUÉS DE EJECUTAR TODOS LOS PASOS:
- El error SQLSTATE[22003] desaparecerá
- Podrás crear citas sin problemas
- El sistema funcionará correctamente
